# Testing Reference

## Unit Tests

Tests live under `test/vcex/`.

Focus areas:

- HTTP parsing.
- WebSocket frame encode/decode.
- Network-safe utilities.
- Room relay behavior.

## Smoke Tests

Use `elixir smoke_test.exs` for a quick live server check.

## Manual Tests

Manual LAN and desktop checks are documented in `VERIFY.md`.

