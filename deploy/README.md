# Deployment

vcex is designed for local and trusted LAN deployment.

## Supported Profiles

- Local process.
- Docker container.
- systemd service on a LAN host.
- Kubernetes lab cluster for experimentation.

Do not expose vcex publicly without adding authentication and reviewing `SECURITY.md`.

