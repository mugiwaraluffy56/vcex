# macOS Packaging

Use the Tauri build pipeline:

```bash
npm run desktop:build
```

Before publishing:

- Confirm camera and microphone permissions.
- Confirm app name and icon.
- Confirm browser fallback still serves.
- Document notarization status.

