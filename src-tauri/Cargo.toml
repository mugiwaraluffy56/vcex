[package]
name = "vcex-desktop"
version = "0.1.0"
description = "Native desktop shell for vcex"
authors = ["vcex"]
edition = "2021"

[lib]
name = "vcex_desktop_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
url = "2"
