# Basic Example

This is the smallest local workflow.

```bash
mix run --no-halt
```

Open two browser tabs:

```text
http://localhost:4000
http://localhost:4000
```

Use this flow for quick signaling checks.

