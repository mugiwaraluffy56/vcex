# Diagnostics Tools

Planned helper scripts:

- Port listener check.
- WebSocket handshake probe.
- LAN IP discovery.
- TURN config checker.

Existing root scripts remain the stable user-facing commands.

