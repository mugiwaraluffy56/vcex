# TURN Relay Example

Use this when the LAN blocks direct peer-to-peer media.

```bash
elixir start_turn.exs
```

Then configure the browser client with local TURN values from `.env.example` or `turnserver.conf`.

