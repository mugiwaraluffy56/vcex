# Linux Packaging

Use the Tauri build pipeline:

```bash
npm run desktop:build
```

Before publishing:

- Confirm WebKit/WebView dependencies.
- Confirm camera and microphone access.
- Confirm desktop launcher metadata.
- Confirm LAN server bind behavior.

