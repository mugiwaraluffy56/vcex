# Latency Benchmarks

## Targets

| Path | Target |
| --- | --- |
| `/health` local | < 100 ms |
| `/` local | < 200 ms |
| WebSocket upgrade local | < 250 ms |
| Offer relay local | < 250 ms |

Record benchmark results with date, machine, OS, and command.

